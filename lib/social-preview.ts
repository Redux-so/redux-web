/** Open Graph / Twitter card image — used site-wide in root metadata. */
export const SOCIAL_PREVIEW_IMAGE = {
  url: "/og-image.jpg",
  width: 1024,
  height: 537,
  alt: "Redux — agentic photo editing platform",
} as const;

export const SOCIAL_PREVIEW_IMAGE_PATH = SOCIAL_PREVIEW_IMAGE.url;
