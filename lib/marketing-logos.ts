import type { StaticImageData } from "next/image";

import reduxLogoText from "@/public/redux-logo-text.png";

/** Navbar / footer wordmark (content-hashed at build). */
export const REDUX_LOGO_TEXT: StaticImageData = reduxLogoText;
